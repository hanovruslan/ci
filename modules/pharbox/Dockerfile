ARG APPROPRIATE_CURL_VERSION
ARG DOCKER_REGISTRY_PATH

FROM ${DOCKER_REGISTRY_PATH}appropriate/curl:${APPROPRIATE_CURL_VERSION}

ARG _CI_IMAGE_CI_DIR
ARG _CI_IMAGE_LOCAL_BIN_DIR
ARG PHARBOX_PATH
ARG PHARBOX_VERSION

COPY resources/ /

RUN sh -x "${_CI_IMAGE_CI_DIR}/image-batch.sh"
